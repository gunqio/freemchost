name: Server Auto Renew

on:
  schedule:
    - cron: '37 6 * * *'
  workflow_dispatch:

jobs:
  renew_job:
    runs-on: ubuntu-latest

    steps:
    - name: 检出基础代码
      uses: actions/checkout@v4

    - name: 设置 Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    # 1. 下载私库脚本 (使用你看到的这种高效写法)
    - name: 下载私有脚本
      env:
        GH_TOKEN: ${{ secrets.GH_PAT }}
      run: |
        mkdir -p scripts
        curl -L -H "Authorization: token $GH_TOKEN" \
          "https://raw.githubusercontent.com/gunqio/zhiyong/main/freemchost.py" \
          -o "scripts/freemchost.py"

    # 2. 启动 Xray 代理并测试
    - name: 启动 Xray 代理
      env:
        V2RAY_CONFIG: ${{ secrets.VMESS_JSON }}
      run: |
        curl -sL https://github.com/XTLS/Xray-core/releases/latest/download/Xray-linux-64.zip -o xray.zip
        unzip -q xray.zip xray
        echo "$V2RAY_CONFIG" > config.json
        ./xray run -c config.json &
        sleep 5
        # 代理测试
        echo "当前代理 IP 为:"
        curl -s --socks5 127.0.0.1:10808 ifconfig.me || echo "代理连接失败，请检查配置"

    # 3. 运行脚本 (安装依赖并执行)
    - name: 运行续期脚本
      env:
        MY_EMAIL: ${{ secrets.MY_EMAIL }}
        MY_PASSWORD: ${{ secrets.MY_PASSWORD }}
        ANON_KEY: ${{ secrets.ANON_KEY }}
        SCKEY: ${{ secrets.SCKEY }}
        # 强制 Python 使用 socks5 代理
        https_proxy: socks5://127.0.0.1:10808
        http_proxy: socks5://127.0.0.1:10808
      run: |
        pip install requests PySOCKS
        python3 scripts/freemchost.py | tee renew.log
