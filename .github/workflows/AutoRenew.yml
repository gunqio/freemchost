name: Server Auto Renew

on:
  schedule:
    - cron: '37 6 * * *'
  workflow_dispatch:

jobs:
  renew_job:
    runs-on: ubuntu-latest

    steps:
    # 1. 使用 PAT 权限拉取私有仓库代码
    - name: Checkout private repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GH_PAT }}

    # 2. 环境准备与 Xray 代理启动
    - name: Setup Environment
      run: |
        python -m pip install --upgrade pip
        pip install requests
        # 下载 Xray 核心
        curl -L https://github.com/XTLS/Xray-core/releases/latest/download/Xray-linux-64.zip -o xray.zip
        unzip xray.zip xray
        # 写入 VMess 配置并启动
        echo '${{ secrets.VMESS_JSON }}' > config.json
        ./xray -c config.json &
        sleep 5

    # 3. 运行根目录下的脚本
    - name: Run freemchost.py
      env:
        MY_EMAIL: ${{ secrets.MY_EMAIL }}
        MY_PASSWORD: ${{ secrets.MY_PASSWORD }}
        ANON_KEY: ${{ secrets.ANON_KEY }}
        SCKEY: ${{ secrets.SCKEY }}
        https_proxy: socks5://127.0.0.1:1080
        http_proxy: socks5://127.0.0.1:1080
      run: |
        # 进入工作区根目录执行
        cd $GITHUB_WORKSPACE
        python freemchost.py | tee renew.log

    # 4. Telegram 通知 (失败时自动提醒)
    - name: Send Telegram Notification
      if: always()
      env:
        TG_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
        TG_ID: ${{ secrets.TG_USER_ID }}
      run: |
        if [ "${{ job.status }}" = "success" ]; then
          TITLE="✅ Freemchost 自动续期成功！"
        else
          TITLE="❌ Freemchost 自动续期失败！"
        fi
        
        # 提取运行日志摘要
        INFO_SUMMARY=$(grep -E "服务器名称|当前状态|新到期时间" renew.log | sed 's/"//g')
        if [ -z "$INFO_SUMMARY" ]; then INFO_SUMMARY="请查看 Action 日志获取详细错误"; fi
        
        MSG_TEXT="${TITLE}%0A%0A📊 状态摘要:%0A${INFO_SUMMARY}"
        
        curl -s -X POST "https://api.telegram.org/bot${TG_TOKEN}/sendMessage" \
          -d "chat_id=${TG_ID}" \
          -d "text=$(echo -e $MSG_TEXT)"
